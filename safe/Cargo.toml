[package]
name = "safe-libsodium"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
name = "sodium"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"
