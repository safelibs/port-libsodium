# Regression Capture Issue Ledger

Phase-2's committed dependent ledger is already green: [dependents/results.tsv](./dependents/results.tsv) contains 16 `PASS` rows and [dependents/failures.list](./dependents/failures.list) is empty. Final hardening keeps those ledgers and their generated evidence directories as the authoritative in-place inputs while moving release closure reporting into the ignored `safe/compat-reports/final/` bundle.

| Package or subsystem | Status | Reproducer location | Resolution note or blocker |
| --- | --- | --- | --- |
| AES-256-GCM detached implementation | FIXED | `cargo check --manifest-path safe/Cargo.toml --release --all-targets`; `safe/tests/ported_pwhash.rs::aes256gcm_detached_in_place_round_trip_preserves_ciphertext_on_auth_failure` | Replaced deprecated `aes_gcm::aead::AeadInPlace` and `from_slice` usage in `safe/src/symmetric_impl.rs` with the current `AeadInOut` + `TryFrom` path. Added a regression for in-place detached round-trips and auth-failure ciphertext preservation. |
| Secretstream warning drift | FIXED | `cargo check --manifest-path safe/Cargo.toml --release --all-targets` | Removed the dead `SECRETSTREAM_COUNTERBYTES` and `SECRETSTREAM_INONCEBYTES` constants from `safe/src/symmetric_impl.rs`, eliminating the remaining project-owned dead-code warnings. |
| qtox headless compatibility coverage | COVERED | `test-original.sh:test_qtox`; generated verification run: `safe/tools/run-dependent-matrix.sh --mode safe --only qtox --report-dir safe/compat-reports/qtox-check` | Full peer-to-peer message exchange still lacks a deterministic headless fixture in the prepared dependent artifacts. The harness now proves more than startup by requiring config-dir creation, persisted log output, signal-handled event-loop shutdown, and byte-for-byte equality between live stdout logging and the persisted qTox log. Supporting logs: `safe/compat-reports/dependents/artifacts/qtox/qtox.log`, generated `safe/compat-reports/qtox-check/artifacts/qtox/startup.log`. |
| Dependent rerun ledger contract | FIXED | `safe/tools/run-dependent-matrix.sh --mode safe --from-list safe/compat-reports/dependents/failures.list`; `safe/tools/run-full-compat.sh` | Safe-mode reruns now default to `safe/compat-reports/dependents-rerun/` when `--from-list` is used, and `run-full-compat.sh` can emit the rerun ledger directly. The required rerun on 2026-04-03 consumed the committed empty phase-2 failure list and wrote the header-only `dependents-rerun/results.tsv` plus an empty `dependents-rerun/failures.list`. |
| Final release bundle hardening | FIXED | `safe/tools/run-full-compat.sh --report-dir safe/compat-reports/final` | The final verifier now consumes the committed durable ledgers in place, rebuilds the release cdylib before ABI/source/relink checks, captures package/build/ABI/source/relink logs plus an `unsafe-audit.txt` into `safe/compat-reports/final/`, validates the Debian package contract, and treats the safe-mode dependent matrix as a hard gate via the generated final ledger. |
